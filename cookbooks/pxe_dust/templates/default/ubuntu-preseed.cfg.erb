d-i time/zone string US/Eastern
d-i clock-setup/utc boolean true
d-i clock-setup/ntp boolean true
d-i mirror/http/proxy string

d-i partman-auto/method string lvm
d-i partman-lvm/confirm boolean true
d-i partman-lvm/device_remove_lvm boolean true
d-i partman-md/device_remove_md boolean true
d-i partman-auto/choose_recipe select atomic
d-i partman/confirm_write_new_label boolean true
d-i partman/confirm_nooverwrite boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true
d-i partman-auto-lvm/guided_size string max

d-i partman-lvm/device_remove_lvm boolean true
d-i partman-md/device_remove_md boolean true

d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean true
d-i debian-installer/splash boolean false

d-i pkgsel/install-language-support boolean false
d-i base-installer/kernel/override-image string linux-server
d-i pkgsel/update-policy select unattended-upgrades
d-i pkgsel/upgrade select safe-upgrade
tasksel tasksel/first multiselect none
d-i pkgsel/include string openssh-server ntp

d-i passwd/user-fullname string <%= @user_fullname %>
d-i passwd/username string <%= @user_username %>
d-i passwd/user-password-crypted password <%= @user_crypted_password %>
d-i user-setup/encrypt-home boolean false

d-i finish-install/reboot_in_progress note

d-i preseed/late_command string wget http://192.168.154.203/default-chef-bootstrap -O /target/etc/default-chef-bootstrap; chroot /target /bin/bash /etc/default-chef-bootstrap;

#d-i preseed/late_command string wget http://192.168.154.203/default-chef-bootstrap -O /etc/aaa1
#d-i preseed/late_command string chroot /target wget http://192.168.154.203/default-chef-bootstrap -O /etc/aaa2
#d-i preseed/late_command string chroot /target mkdir -p /etc/bootstrap; wget --no-proxy -o /etc/bootstrap/log -O /etc/bootstrap/chef-bootstrap http://<%= node[:ipaddress] %>/<%= @id %>-chef-bootstrap;
#d-i preseed/late_command string chroot /target /bin/bash /etc/bootstrap/chef-bootstrap
